const Header = ({className}) => {
    return (
        <header>
            <h1 className={className}>
                zeltserj To Do List
            </h1>
        </header>
        
    )
}

export default Header;